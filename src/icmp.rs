use std::net::IpAddr;
use std::time::Duration;

fn checksum(data: [u8]) -> u16 {
    todo!()
}

fn build_echo_request(id: u16, seq: u16) -> Vec<u16> {
    todo!()
}

fn ping_host(ip: IpAddr, timeout: Duration) -> bool {
    todo!()
}
